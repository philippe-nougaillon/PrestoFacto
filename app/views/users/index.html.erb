<div class="pb-2 mt-4 mb-2 border-bottom">
  <% if policy(User).new? %>
    <%= link_to new_users_admin_path, class: 'btn btn-outline-primary float-end' do %>
      <%= fa_icon 'plus-circle' %> Ajouter un Utilisateur
    <% end %>
  <% end %>

  <h2><%= fa_icon 'user-friends' %> Utilisateurs</h2>
</div>

<%= bootstrap_form_tag url: users_admin_index_path, method: :get do |f| %>
  <div class="row">
    <div class="col"><%= f.text_field :email, value: params[:email], onchange: "this.form.submit()", autofocus: false %></div>
  </div>
<% end %>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Email</th>
      <th>Rôle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tfoot><tr><th colspan=4></th></tr></tfoot>

  <tbody>
    <%= render @users %>
  </tbody>
</table>

<%= page_entries_info @users, entry_name: 'utilisateur' %>
<%= paginate @users, theme: 'twitter-bootstrap-4' %>
