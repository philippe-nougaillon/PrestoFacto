<div class="pb-2 mt-4 mb-2 border-bottom">
  <% if policy(User).new? %>
    <%= link_to new_users_admin_path, class: 'btn text-info float-right' do %>
      <i class="fas fa-plus-circle"></i> Utilisateur
    <% end %>
  <% end %>

  <h2><i class="fas fa-user-friends"></i> Utilisateurs</h2>
</div>

<%= bootstrap_form_tag url: users_admin_index_path, method: :get do |f| %>
  <div class="row">
    <div class="col"><%= f.text_field :email, value: params[:email], onchange: "this.form.submit()" %></div>
  </div>
<% end %>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Email</th>
      <th>Rôle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tfoot><tr><% 4.times do %><th></th><% end %></tr></tfoot>

  <tbody>
    <%= render @users %>
  </tbody>
</table>

<div class="text-secondary">
  <%= page_entries_info @users, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
</div>

<%= will_paginate @users, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
<br>