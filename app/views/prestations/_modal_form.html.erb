<%= bootstrap_form_with(url: prestations_path, model: prestation, method: :post, local: true) do |form| %>

    <%= form.hidden_field :enfant_id %>

    <div class="form-inline">
        <%= form.text_field :date, label: 'Le :', type: "date" %>
    </div>
    <br>
    
    <%= form.select :prestation_type_id, options_from_collection_for_select(current_user.organisation.prestation_types, 'id', 'nom', prestation.prestation_type_id), { label: "Prestation type", wrapper: { class: 'has-warning', data: { foo: 'bar' } } }, { class: "selectpicker" } %>
    <%= form.number_field :qté, value: 1 %>

    <%= form.submit "Enregistrer", class: "btn btn-success" %>

<% end %>
