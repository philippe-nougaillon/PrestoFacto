<div class="w-full md:w-10/12 px-2 lg:w-7/12 shadow-sm rounded-lg p-1 md:p-4 mt-12 mx-auto bg-base-100">
  <h1 class="text-center">Nous contacter</h1>
  <div class="text-center">
    <p class="text-lg font-light text-base">
      Vous avez une question ou vous souhaitez que l'on vous contacte ?
      <br>
      Vos remarques et propositions sont les bienvenues 
      et nous aident à améliorer l'application.
    </p>
  </div>

  <%= form_with(model: message, local: true) do |form| %>

    <%= form.hidden_field :facture_slug %>
    <%= form.hidden_field :organisation_id %>

    <% unless params[:facture_slug] %>
      <fieldset class="fieldset">
        <%= form.label :email, class: "fieldset-legend" %>
        <%= form.email_field :email, placeholder: 'Votre adresse e-mail', required: true, class: "input w-full" %>
      </fieldset>
      <fieldset class="fieldset">
        <%= form.label :objet, class: "fieldset-legend" %>
        <%= form.select :objet, options_for_select(['Question', 'Offre standard', 'Offre premium', 'Remarque', "Proposition d'amélioration", 'Résiliation', 'Autre'], @offre), { prompt: 'Choisir un objet', required: true }, { class: "select w-full" } %>
      </fieldset>
    <% else %>
      <fieldset class="fieldset">
        <%= form.label :email, class: "fieldset-legend" %>
        <%= form.email_field :email, placeholder: 'Votre adresse e-mail', required: true, class: "input w-full" %>
      </fieldset>
      <fieldset class="fieldset">
        <%= form.label :objet, class: "fieldset-legend" %>
        <%= form.select :objet, options_for_select(["Facture n° #{@facture.réf}", "Question", "Remarque", "Autre"]), { required: true }, { class: "select w-full" } %>
      </fieldset>
    <% end %>
    <fieldset class="fieldset">
      <%= form.label :contenu, class: "fieldset-legend" %>
      <%= form.text_area :contenu, placeholder: 'Votre message', rows: 4, required: true, class: "textarea w-full" %>
    </fieldset>

    <%= recaptcha_tags unless Rails.env.development? %>

    <br>

    <div class="actions">
      <%= form.submit 'Envoyer le message', class: 'btn btn-outline btn-success' %>
      |
      <%= link_to 'Annuler', root_path, class: 'btn btn-outline btn-secondary'%>
    </div>
  <% end %>
</div>