<%= form_with(url: absences_path, model: absence, method: :post, local: true) do |form| %>

  <%= form.hidden_field :enfant_id %>

  <div class="">
    <fieldset class="fieldset">
      <%= form.label :début, 'Du :', class: "fieldset-legend" %>
      <%= form.text_field :début, type: "date", class: "input input-bordered w-full" %>
    </fieldset>
    <fieldset class="fieldset">
      <%= form.label :fin, 'Au :', class: "fieldset-legend" %>
      <%= form.text_field :fin, type: "date", class: "input input-bordered w-full" %>
    </fieldset>
  </div>
  <br />

  <div class="custom-control">
    <fieldset class="fieldset">
      <%= form.label :matin, class: "label" do %>
        <%= form.check_box :matin, class: "checkbox checkbox-primary" %>
        Matin
      <% end %>
    </fieldset>
    <fieldset class="fieldset">
      <%= form.label :midi, class: "label" do %>
        <%= form.check_box :midi, class: "checkbox checkbox-primary" %>
        Midi
      <% end %>
    </fieldset>
    <fieldset class="fieldset">
      <%= form.label :soir, class: "label" do %>
        <%= form.check_box :soir, class: "checkbox checkbox-primary" %>
        Soir
      <% end %>
    </fieldset>
  </div>
  <br />

  <%= form.submit "Enregistrer", class: "btn btn-outline btn-success" %>

<% end %>
