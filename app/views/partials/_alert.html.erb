<% if notice.present? or alert.present? %>
  <div class="alert alert-dismissible fade show alert-<%= notice ? 'success' : 'danger' %>" role="alert">
    <%= notice ? notice : alert %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>