<%= bootstrap_form_with(model: prestation_type, local: true) do |form| %>

  <%= form.hidden_field :organisation_id %>
  <%= form.text_field :nom, required: true %>

  <%= form.submit "Enregistrer", class: "btn btn-outline-success" %>

<% end %>