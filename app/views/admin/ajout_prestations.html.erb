<div class="pb-2 mt-4 mb-2 border-bottom">
  <h2><%= fa_icon 'calendar-day' %> Comptabilisation des prestations</h2>
</div>

<p>
    <h4>Permet de lancer la routine journalière de comptabilisation des prestations consommées</h4>
    <small>Cette fonction balaye l'ensemble des réservations prévues le <%= l Date.today - 1.day %>
            et crée les prestations idoines si les paramètres concordes (jour de la semaine, Scolaire/HorsScolaire, etc.)
    </small>
</p>
<br />

<%= bootstrap_form_with(url: 'ajout_prestations_do', local: true) do |form| %>
    <%= form.check_box :enregistrer, {custom: :switch, label: "Enregistrer les changements ? (non: visualisation seulement, ne fait rien de définitif :)"} %>  
    <br /><br />
    <%= form.submit "Lancer", class: "btn btn-success" %>
<% end %>


