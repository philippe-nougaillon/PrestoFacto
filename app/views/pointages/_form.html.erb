<%= form_with(model: pointage) do |form| %>
  <% if pointage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pointage.errors.count, "error") %> prohibited this pointage from being saved:</h2>

      <ul>
        <% pointage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <strong>Enfant :</strong>
    <%= pointage.enfant.nom_et_prénom %>
  </p>
  <p>
  <strong>Type de prestation :</strong>
    <%= pointage.prestation_type.nom %>
  </p>
  <p>
    <strong>Date de pointage :</strong>
    <%= l pointage.date_pointage %>
  </p>
  <div class="flex flex-wrap -mx-2">
    <div class="w-full sm:w-3/12 px-2"><%= form.datetime_field :heure_pointage %></div>
  </div>
  <div class="flex flex-wrap -mx-2">
    <div class="w-full sm:w-3/12 px-2"><%= form.submit "Enregistrer", class: "btn btn-outline btn-success w-full mt-6" %></div>
  </div>
<% end %>
