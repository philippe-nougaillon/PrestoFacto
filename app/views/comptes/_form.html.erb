<%= form_with(model: compte, local: true, data: { controller: 'nested-form', nested_form_wrapper_selector_value: '.nested-form-wrapper' }) do |form| %>
  <% if compte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compte.errors.count, "erreur") %> empêche(nt) l'enregistrement du compte :</h3>

      <ul>
        <% compte.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :organisation_id %>

  <div class="flex flex-wrap -mx-2">
    <div class="w-full sm:w-6/12 px-2 mb-4">
      <div class="flex flex-wrap -mx-2">
        <div class="w-full sm:w-9/12 px-2">
          <fieldset class="fieldset">
            <%= form.label :nom, "Nom *", class: "fieldset-legend" %>
            <%= form.text_field :nom, required: true, autofocus: false, class: "input w-full" %>
          </fieldset>
        </div>
        <div class="w-full sm:w-3/12 px-2">
          <fieldset class="fieldset">
            <%= form.label :civilité, class: "fieldset-legend" %>
            <%= form.select :civilité, ["M et Mme","Mme", "M"], {}, { class: "select w-full" } %>
          </fieldset>
        </div>
      </div>

      <div class="flex flex-wrap -mx-2">
        <div class="flex-1 px-2">
          <fieldset class="fieldset">
            <%= form.label :adresse1, "Adresse", class: "fieldset-legend" %>
            <%= form.text_field :adresse1, class: "input w-full" %>
          </fieldset>
        </div>
        <div class="flex-1 px-2">
          <fieldset class="fieldset">
            <%= form.label :adresse2, "Adresse complémentaire", class: "fieldset-legend" %>
            <%= form.text_field :adresse2, class: "input w-full" %>
          </fieldset>
        </div>
      </div>

      <div class="flex flex-wrap -mx-2">
        <div class="w-full sm:w-3/12 px-2">
          <fieldset class="fieldset">
            <%= form.label :cp, class: "fieldset-legend" %>
            <%= form.text_field :cp, class: "input w-full" %>
          </fieldset>
        </div>
        <div class="w-full sm:w-9/12 px-2">
          <fieldset class="fieldset">
            <%= form.label :ville, class: "fieldset-legend" %>
            <%= form.text_field :ville, class: "input w-full" %>
          </fieldset>
        </div>
      </div>
      <fieldset class="fieldset">
        <%= form.label :num_allocataire, "Numéro d'Allocataire", class: "fieldset-legend" %>
        <%= form.text_field :num_allocataire, class: "input w-full" %>
      </fieldset>
      <fieldset class="fieldset">
        <%= form.label :mémo, class: "fieldset-legend" %>
        <%= form.text_area :mémo, class: "textarea w-full" %>
      </fieldset>
      <%= check_box_tag :acces, '1', false, class: 'checkbox checkbox-primary' %>
      <%= label_tag :acces, "Créer automatiquement un compte d'accès au portail parents pour les contacts à prévenir en cas d'urgence et leur envoyer une notification ?", class:'inline' %>
    </div>

    <div class="w-full sm:w-6/12 px-2">
      <h5>
        Contacts
      </h5>
      <template data-nested-form-target="template">
        <%= form.fields_for :contacts, Contact.new, child_index: 'NEW_RECORD' do |contact_fields| %>
          <%= render "contact_form", f: contact_fields %>
        <% end %>
      </template>

      <%= form.fields_for :contacts do |contact_fields| %>
        <%= render "contact_form", f: contact_fields %>
      <% end %>

      <!-- Inserted elements will be injected before that target. -->
      <div data-nested-form-target="target"></div>

      <button class="btn btn-outline btn-primary" type="button" data-action="nested-form#add"><%= mi_icon 'plus-circle' %> Ajouter un contact</button>
    </div>
  </div>

  <p>* : Champs requis</p>

  <div class="mb-4">
    <%= form.submit "Enregistrer", class: "btn btn-outline btn-success" %>
  </div>

<% end %>
