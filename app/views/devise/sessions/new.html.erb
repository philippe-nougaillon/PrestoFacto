<div class="col-md-10 col-lg-7 shadow-sm rounded p-3 p-md-5 mx-auto">
  <h1 class="fw-light mt-2 mb-5 text-center">Connexion</h1>
  <%= bootstrap_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <%= f.email_field :email, autofocus: false, autocomplete: "email", class: "form-control" %>
    <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>

    <% if devise_mapping.rememberable? %>
      <div class="checkbox">
        <%= f.check_box :remember_me, custom: :switch %>
      </div>
    <% end %>
    <br>

    <div class="row">
      <div class="col">
        <%= f.submit "Se connecter", class: "btn btn-outline-success w-100" %>
      </div>
      <div class="col">
        <%= link_to "S'inscrire", new_user_registration_path, class: "btn btn-outline-primary w-100", 'data-turbo': false %> 
      </div>
    </div>
  <% end %>
  <br>

  <%= render "devise/shared/links" %>
</div>